<Window x:Class="AuraFileManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2003/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AuraFileManager.ViewModels"
        mc:Ignorable="d"
        Title="Aura File Manager" Height="720" Width="1280"
        Background="#09090b"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#0d0d0e" Grid.Column="0" BorderBrush="#1a1a1c" BorderThickness="0,0,1,0">
            <StackPanel Margin="20,40,20,20">
                <TextBlock Text="AURA MANAGER" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,30"/>
                
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                         Background="#161618" Foreground="White" BorderThickness="1" BorderBrush="#2a2a2c"
                         Padding="10,8" Margin="0,0,0,20" />

                <RadioButton Content="All Files" IsChecked="True" Style="{StaticResource NavButton}" Command="{Binding ChangeCategoryCommand}" CommandParameter="All"/>
                <RadioButton Content="Word Files" Style="{StaticResource NavButton}" Command="{Binding ChangeCategoryCommand}" CommandParameter="Word Files"/>
                <RadioButton Content="PDF &amp; E-books" Style="{StaticResource NavButton}" Command="{Binding ChangeCategoryCommand}" CommandParameter="PDF &amp; E-books"/>
                <RadioButton Content="Videos" Style="{StaticResource NavButton}" Command="{Binding ChangeCategoryCommand}" CommandParameter="Videos"/>
            </StackPanel>
        </Border>

        <!-- Main Workspace -->
        <Grid Grid.Column="1" Background="#0c0c0e">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <Border BorderBrush="#1a1a1c" BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <Button Content="Sort by Size" Background="Transparent" Foreground="#666" BorderThickness="0" Margin="0,0,15,0" />
                    <Button Content="Sort by Date" Background="Transparent" Foreground="#666" BorderThickness="0" />
                </StackPanel>
            </Border>

            <!-- File Grid -->
            <ScrollViewer Grid.Row="1" Padding="20">
                <ItemsControl ItemsSource="{Binding FilteredFiles}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="180" Height="220" Margin="10" CornerRadius="12" Background="#141416" BorderBrush="#1a1a1c" BorderThickness="1">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Image Source="/Assets/file_icon.png" Width="64" Height="64" Margin="0,0,0,15"/>
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="13" FontWeight="SemiBold" TextAlignment="Center" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding FormattedSize}" Foreground="#555" FontSize="11" Margin="0,5,0,0" TextAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
